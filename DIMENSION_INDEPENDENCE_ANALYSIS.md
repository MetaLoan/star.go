# 维度独立性分析与解决方案

## 问题描述

五个维度的曲线趋势完全同步，虽然数值不同，但波峰和波谷出现在相同的时间点。

## 数学根本原因

### 当前计算模型

```
维度分数(t) = 基础分 + Σ[因子_i(t) × 该维度的影响权重_i]

Career(t) = 50 + f1(t)×w_c1 + f2(t)×w_c2 + ... + fn(t)×w_cn
Spiritual(t) = 50 + f1(t)×w_s1 + f2(t)×w_s2 + ... + fn(t)×w_sn
```

### 问题所在

**所有维度共享相同的因子集合 {f1, f2, ..., fn}**

- 当因子 fi 在时刻 t1 出现/消失时
- 所有维度都会在 t1 同时发生变化
- 虽然变化的方向和幅度不同（权重 w 不同）
- 但**变化的时间节点完全同步**

### 实际数据验证

```
总活跃因子：16个
每个维度受影响因子：4个

所有维度都受相同的4个因子影响：
- Jupiter Retrograde
- Uranus Retrograde  
- North Node Retrograde
- Balsamic Moon

结果：所有维度的曲线在相同时刻出现拐点
```

## 数学解决方案

### 方案1：维度专属因子集合（推荐）

让不同维度受到**不同的因子子集**影响：

```
Career(t) = 50 + f1(t)×w1 + f2(t)×w2 + f3(t)×w3
Spiritual(t) = 50 + f4(t)×w4 + f5(t)×w5 + f6(t)×w6

其中 {f1, f2, f3} ∩ {f4, f5, f6} 可能为空或很小
```

**实现方式：因子过滤器**

根据因子类型和行星属性，判断是否影响某个维度：

| 因子 | Career | Finance | Health | Relationship | Spiritual |
|------|--------|---------|--------|--------------|-----------|
| 水星逆行 | ✓ | ✗ | ✗ | ✓ | ✗ |
| 木星逆行 | ✗ | ✓ | ✗ | ✗ | ✓ |
| 月相 | ✗ | ✗ | ✗ | ✓ | ✓ |
| 天王星逆行 | ✗ | ✗ | ✗ | ✗ | ✓ |

### 方案2：时间延迟效应

不同维度对同一因子的响应速度不同：

```
Career(t) = 50 + Σ[因子_i(t) × 权重_i]  // 立即响应
Health(t) = 50 + Σ[因子_i(t-0.5天) × 权重_i]  // 延迟0.5天
Spiritual(t) = 50 + Σ[因子_i(t-1天) × 权重_i]  // 延迟1天
```

### 方案3：维度间相互作用（高级）

引入维度之间的相关性：

```
Career(t) = f(基础分, 因子, -0.2×Relationship(t))  // 关系好会轻微影响事业
Spiritual(t) = f(基础分, 因子, +0.3×Health(t))  // 健康影响灵性
```

## 实现状态

### 已完成

1. ✅ 创建维度过滤器 (`dimension_factor_filter.go`)
2. ✅ 定义过滤规则（基于行星和因子类型）
3. ✅ 集成到 `GetFactorDimensionImpact` 函数

### 待优化

1. **过滤规则需要更激进** - 当前还是有较多因子影响所有维度
2. **增加更多因子类型** - 让某些因子只影响1-2个维度
3. **调整V2影响映射** - 在 `factor_dimension_impacts_v2.go` 中增加更多 0 值

## 推荐行动方案

### 短期（立即）

修改 `factor_dimension_impacts_v2.go`，增加更多零值：

```go
// 例如：月相主要影响情绪
models.FactorLunarPhase: {
    Career:       0.00,  // 不影响
    Relationship: +0.40, 
    Health:       0.00,  // 不影响
    Finance:      0.00,  // 不影响
    Spiritual:    +0.50,
}

// 工作相关的土星相位
models.FactorAspectPhase (Saturn): {
    Career:       +0.60,
    Relationship: 0.00,
    Health:       0.00,
    Finance:      0.00,
    Spiritual:    0.00,
}
```

### 中期（本周）

1. 为不同行星的相位设置不同的维度过滤规则
2. 增加基于宫位的过滤（1宫/10宫 → Career，7宫 → Relationship）
3. 调整逆行因子的影响，让外行星逆行只影响灵性

### 长期（下月）

1. 实现时间延迟效应
2. 引入维度间相互作用
3. 添加更多专属因子类型（健康因子、财务因子等）

## 预期效果

应用这些方案后，五个维度的曲线将呈现：
- **不同的波峰/波谷时间点**
- **独立的上升/下降趋势**
- **真正的维度独立性**

示意图：
```
Career:       ↗️ ↘️ ↗️ ↘️ ↗️
Relationship: ↘️ ↗️ ↗️ ↘️ ↗️  (不同节奏)
Spiritual:    ↗️ ↗️ ↘️ ↗️ ↘️  (独立走势)
```
