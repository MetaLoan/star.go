1、业务层/逻辑层/计算层/因子层/前端/算法/API全部独立分层解耦合，API数据互通使用json
2、前端用react，后端用go
3、为多平台多客户端进行提前规划架构
4、前端设计标准组件化，组件命名规范：组件名+效果名（如有）+4位随机数字+2位变体字母，尽量复用/直接调用组件和变体组件。
5、前端设计颜色/渐变/字体/边距/等样式标准化。尽量直接调用。
6、尽量避免重复造车轮，业务层/逻辑层如果有现成的git库，优先使用
7、如果你有觉得不合理的地方可以随时提出。