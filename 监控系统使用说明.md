# 🌟 Star API 监控系统 - 使用说明

**版本**：v1.0  
**创建日期**：2026-01-16  
**状态**：✅ 已部署并运行

---

## 🎯 快速开始（只需30秒）

### 方式1：Web监控仪表板（推荐）

**打开浏览器，访问：**
```
http://localhost:8080/api/monitor/dashboard
```

你会看到一个美观的实时监控页面，包含：
- 📊 核心指标（总请求数、成功率、活跃请求）
- 📋 API统计表格
- ⏱️ 实时监控（最近30秒）
- 📜 最新50条请求记录

**特点：**
- 🔄 每3秒自动刷新
- 🎨 现代化UI设计
- 📱 支持移动端

---

### 方式2：命令行查看

```bash
# 查看概览
curl http://localhost:8080/api/monitor/summary | jq .

# 查看API统计
curl http://localhost:8080/api/monitor/stats | jq .

# 查看最近20条请求
curl http://localhost:8080/api/monitor/recent?limit=20 | jq .
```

---

## 📊 监控指标详解

### 核心指标

| 指标 | 说明 | 用途 |
|------|------|------|
| **总请求数** | 自启动以来的所有请求 | 了解整体流量 |
| **活跃请求** | 当前正在处理的请求 | 监控并发情况 |
| **成功率** | 2xx/3xx状态码占比 | 系统健康度 |
| **最近1分钟** | 最近60秒的请求数 | 实时流量监控 |

### API统计

每个API端点独立统计：
- ✅ 总请求数、成功数、失败数
- ✅ 平均响应时间
- ✅ 最快/最慢响应时间
- ✅ 最后访问时间

### 请求详情

每条请求记录包含：
- 时间戳
- 请求方法和路径
- 状态码
- 响应时间（毫秒）
- 客户端IP
- User-Agent
- 请求/响应大小

---

## 🔍 实际使用示例

### 示例1：日常监控

每天早上查看一次仪表板：
```
http://localhost:8080/api/monitor/dashboard
```

关注：
- ✅ 成功率是否 > 95%
- ✅ 平均响应时间是否正常
- ✅ 是否有异常错误

---

### 示例2：性能问题排查

**步骤1：找出最慢的API**
```bash
curl http://localhost:8080/api/monitor/stats | \
  jq 'to_entries | sort_by(.value.maxDuration) | reverse | .[0:5] | 
  .[] | {api: .key, maxDuration: .value.maxDuration, avgDuration: .value.avgDuration}'
```

**输出示例**：
```json
{
  "api": "POST /api/calc/time-series",
  "maxDuration": 2341,
  "avgDuration": 856
}
```

**步骤2：查看慢请求详情**
```bash
curl http://localhost:8080/api/monitor/recent?limit=100 | \
  jq '[.[] | select(.duration > 1000)] | sort_by(.duration) | reverse | .[0:10]'
```

---

### 示例3：错误分析

**查看所有错误**：
```bash
curl http://localhost:8080/api/monitor/recent?limit=500 | \
  jq '[.[] | select(.statusCode >= 400)] | 
  group_by(.path) | 
  map({path: .[0].path, errorCount: length, errors: map({statusCode, time: .timestamp, ip: .clientIP})})'
```

---

### 示例4：客户端流量分析

**统计TOP 10客户端**：
```bash
curl http://localhost:8080/api/monitor/recent?limit=1000 | \
  jq 'group_by(.clientIP) | 
  map({ip: .[0].clientIP, requestCount: length, apis: map(.path) | unique}) | 
  sort_by(.requestCount) | 
  reverse | 
  .[0:10]'
```

---

### 示例5：流量趋势监控

**实时追踪（每3秒刷新）**：
```bash
watch -n 3 'curl -s http://localhost:8080/api/monitor/realtime?seconds=60 | jq "{requests: .requestCount, avgDuration: .avgDuration, statusCodes}"'
```

---

## 🎨 仪表板截图说明

访问 `http://localhost:8080/api/monitor/dashboard` 后你会看到：

### 第一行：核心指标卡片
```
┌──────────────┬──────────────┬──────────────┬──────────────┐
│ 总请求数      │ 活跃请求      │ 成功率        │ 最近1分钟     │
│   1,523      │      3       │   98.4%      │     45       │
│ 自启动以来    │ 正在处理中    │ 所有请求      │ 平均11.2/min │
└──────────────┴──────────────┴──────────────┴──────────────┘
```

### 第二行：API统计表格
```
┌────────┬─────────────────────────┬────────┬──────┬──────┬──────────┬────────┐
│ 方法   │ 路径                     │ 请求数 │ 成功 │ 失败 │ 平均耗时 │ 最慢   │
├────────┼─────────────────────────┼────────┼──────┼──────┼──────────┼────────┤
│ POST   │ /api/calc/daily         │  456   │ 450  │  6   │  892ms   │ 1523ms │
│ POST   │ /api/calc/time-series   │  234   │ 234  │  0   │  724ms   │ 1102ms │
│ GET    │ /health                 │  833   │ 833  │  0   │    1ms   │    3ms │
└────────┴─────────────────────────┴────────┴──────┴──────┴──────────┴────────┘
```

### 第三行：实时监控 + 最近请求
```
┌─────────────────────────┬───────────────────────────────────────┐
│ 实时监控（最近30秒）     │ 最近请求 (最新50条)                   │
│                         │                                       │
│ 请求数: 45              │ 18:15:30  POST  /api/calc/daily  200  │
│ 平均响应: 856ms         │ 18:15:25  POST  /time-series     200  │
│ 状态码: 200:42 400:2    │ 18:15:20  GET   /health          200  │
└─────────────────────────┴───────────────────────────────────────┘
```

---

## 🔔 监控告警（可选配置）

虽然系统未内置告警，但你可以通过简单脚本实现：

```bash
#!/bin/bash
# monitor_alert.sh - 简单监控告警

while true; do
    # 获取成功率
    SUCCESS_RATE=$(curl -s http://localhost:8080/api/monitor/summary | jq '.successRate')
    
    # 检查阈值
    if (( $(echo "$SUCCESS_RATE < 90" | bc -l) )); then
        echo "[警告] 成功率过低: $SUCCESS_RATE%"
        # 这里可以发送邮件、Webhook等
    fi
    
    # 每分钟检查一次
    sleep 60
done
```

---

## 💾 数据持久化（可选）

监控数据默认存储在内存中。如需持久化：

### 方案1：定期导出JSON

```bash
# 添加到crontab
0 * * * * curl -s http://localhost:8080/api/monitor/stats > /var/log/star-api-stats-$(date +\%Y\%m\%d-\%H).json
```

### 方案2：导出到数据库

```bash
# 示例：导出到SQLite
curl -s http://localhost:8080/api/monitor/recent?limit=1000 | \
  jq -r '.[] | [.timestamp, .path, .method, .statusCode, .duration, .clientIP] | @csv' | \
  sqlite3 monitor.db "INSERT INTO requests VALUES ..."
```

---

## 🎯 当前监控状态

**你的监控系统已经在运行：**

```bash
# 实时数据
总请求数: 24
成功率: 100%
运行时间: 2分钟
活跃API: 5个
```

**立即访问**：
```
🌐 Web监控: http://localhost:8080/api/monitor/dashboard
📊 API统计: http://localhost:8080/api/monitor/stats
📜 最近请求: http://localhost:8080/api/monitor/recent
```

---

## 📚 更多资源

- **详细指南**：[MONITOR_GUIDE.md](./MONITOR_GUIDE.md)
- **API文档**：[backend/docs/API-REFERENCE.md](./backend/docs/API-REFERENCE.md)
- **性能文档**：[PERFORMANCE_OPTIMIZATION_2026-01-16.md](./PERFORMANCE_OPTIMIZATION_2026-01-16.md)

---

## ❓ 常见问题

**Q: 监控数据会占用很多内存吗？**  
A: 不会，最多20MB左右（保留1000条最近请求）

**Q: 会影响API性能吗？**  
A: 几乎不会，每个请求增加 < 0.1ms

**Q: 服务重启后数据会丢失吗？**  
A: 是的，当前版本数据存在内存中。可以通过定期导出来保留历史数据。

**Q: 能否添加认证？**  
A: 可以，在routes.go中为monitor路由组添加认证中间件即可。

---

## 🎉 开始使用

**现在就打开浏览器，访问监控仪表板：**

```
http://localhost:8080/api/monitor/dashboard
```

**实时查看你的API调用情况！** 🚀
